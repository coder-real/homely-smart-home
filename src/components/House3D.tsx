// House3D is no longer used in the current design.
// Keeping this file as a stub to avoid breaking the build.
import React from 'react';
import { View } from 'react-native';
export default function House3D() {
  return <View />;
}
